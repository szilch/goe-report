version: 2

project_name: goe-report

before:
  hooks:
    - go mod tidy

release:
  prerelease: auto

builds:
  - id: default
    main: ./main.go
    binary: goe-report
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64

archives:
  - formats:
      - tar.gz
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    format_overrides:
      - goos: windows
        formats:
          - zip

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
  groups:
    - title: Features
      regexp: "^.*feat[(\\w)]*:+.*$"
      order: 0
    - title: "Bug Fixes"
      regexp: "^.*fix[(\\w)]*:+.*$"
      order: 1
    - title: Others
      order: 999

dockers_v2:
  - image_templates:
      - "ghcr.io/szilch/goe-report:{{ .Tag }}"
      - "ghcr.io/szilch/goe-report:v{{ .Major }}"
      - "ghcr.io/szilch/goe-report:latest"
    goos: linux
    goarch:
      - amd64
      - arm64
    dockerfile: docker/Dockerfile
    extra_files:
      - docker/entrypoint.sh
    build_flag_templates:
      - "--target=goreleaser"
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.name={{.ProjectName}}"
      - "--label=org.opencontainers.image.revision={{.FullCommit}}"
      - "--label=org.opencontainers.image.version={{.Version}}"
